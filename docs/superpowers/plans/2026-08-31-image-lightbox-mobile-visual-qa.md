# Image Lightbox Mobile Visual QA Plan

Date: 2026-08-31

## 1. Freeze exact data, interactions, and callback results

- Fix legal arrays at 1 through 10 with in-range indices.
- Fix `42 = 12 canonical + 30 interactions`, direct-only registration, and totals `452/1,010`, `558`, `70,947`, `71,950`.
- Freeze exact-one close/previous/next activations and exact-zero non-actions/lifecycle checks before reset.

## 2. TDD actual product behavior

- Add failing Chromium tests at 320/390 for dialog semantics, focus trap/restore, exact scroll lock restoration, Korean labels, 44px controls, reduced obstruction, containment, counters/wrap, broken fallback/reset, propagation, key cleanup, and callback ledgers.
- Prove fast close remains one and a single previous/next DOM event never dispatches twice.
- Apply only minimal leaf-local product fixes justified by RED.
- Immediately run isolated Vite compile, CSS non-growth, and all 153 budgets without writing main reports.

## 3. TDD exact adapter and companion

- Add exact tests for twelve canonical and thirty interaction scenarios.
- Implement one lower-camel controlled QA companion with exact original mapping.
- Fail closed for all unapproved data/index/theme/interaction/target/action/module combinations.
- Prove QA-only paths and IDs are absent from production chunks.

## 4. Register arithmetic and package regression

- Change only ImageLightbox direct manifest status to registered.
- Verify final totals and zero host/root changes.
- Add the actual test exactly once to pre-harness; prove package omission and duplication RED and record post pre/full counts.

## 5. Capture exact evidence

- Warm up outside final paths using local/data assets only.
- Capture exactly 42 rows at 320x844, fresh/expand-tall-flow.
- Verify f0/r0, attempt1, interaction30 flags, unique/nonzero/hash-complete artifacts, report SHA, repo-standard PNG aggregate, exact legal equal groups, and zero external image requests.
- Verify prior reports/PNGs and root evidence diff zero.

## 6. Regression and clean closure

- Run focused actual/mutations, focus/icon regressions, adapter/catalog/contracts, state arithmetic, pre/full harness, production isolation/import/audit/reflow, DOM probe, TypeScript, isolated compile/CSS/budgets, Priority 0, and scoped diff checks.
- Reproduce actual behavior, exact totals, TypeScript, build/CSS/isolation, and artifact hashes from final HEAD without writing main reports.
- Finish with index zero, owned diff zero, temporary paths removed, and all concurrent/generated-report paths preserved.

## 7. Independent review

- A separate reviewer checks matrix arithmetic, reset-before-assert ledgers, mutation effectiveness, focus/body/error lifecycle, touch/obstruction, evidence and external-request isolation, package/build/CSS, clean scope, concurrency, and policy.
- Critical or Important findings return to the original implementer. Product, manifest, or interaction-output changes require fresh affected evidence.
