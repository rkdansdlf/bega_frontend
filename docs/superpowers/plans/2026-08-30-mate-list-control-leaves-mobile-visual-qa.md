# Mate List Control Leaves Mobile Visual QA Plan

Date: 2026-08-30

## 1. Freeze matrices and host IDs

- Fix Status `21`, Sort `30`, Seat `28`, Status hosted `4`, Sort hosted `4`, and Mate root `6` IDs.
- Preserve Seat hosted pending and all controller/API/query/auth semantics.
- Record the main-worktree CSS 35-byte starting headroom and forbid new utilities.

## 2. TDD leaf product contracts

- Add failing actual-mount tests for Status 44px, Sort 320/390 viewport containment, and Seat rail/toolbar overflow/reachability.
- Add target test IDs/ARIA, reuse emitted pressed utilities, change Status to `h-11`, and Sort to `align=start` only.
- Run build and all 153 budgets immediately after first product GREEN.

## 3. TDD stateful direct adapters

- Add failing tests for the three direct IDs, exact matrix dimensions, legal combinations, and fail-closed unknown targets.
- Implement production-excluded stateful wrappers for Status/Seat/Sort controlled selection.
- Prove actual public callback identity/value/count, unrelated zero, reciprocal miswire RED, and resize/open/scroll no replay.

## 4. Register hosted aliases and exact totals

- Register Status and Sort hosted aliases with the same four existing Mate host IDs.
- Keep Seat hosted and MateListControlsRuntime direct pending.
- Verify registered `445/1,010`, pending `565`, direct `70,712`, valid `71,715`.

## 5. Fresh evidence

- Warm up outside final evidence.
- Capture Status `21/21`, Sort `30/30`, Seat `28/28`, and replacement Mate root `6/6` at 320x844 fresh/expand-tall-flow.
- Verify failed/recovered zero, attempts one, artifact/hash integrity, interaction result/callback evidence, meaningful SHA distinctions, and hosted mappings.

## 6. Regression, clean closure, and release gates

- Run focused Mate leaf/runtime/controller and prior duplicate-call coordinator tests.
- Run contracts, inventory, state generation, pre/full harness, isolation/import/audit/reflow, DOM probe, TypeScript, build, all 153 budgets, Priority 0 policy, and diff checks.
- Verify a clean checkout has all new leaf/catalog/test dependencies; do not rely on remaining staged or concurrent Landing files.
- Preserve remaining staged 99 paths except verified prerequisites that are explicitly consumed and documented.

## 7. Independent review

- A separate reviewer checks mutation-resistant actual callbacks, touch/menu/overflow/focus behavior, exact direct/hosted/root state math, report/PNG integrity, CSS budget, clean scope, and policy.
- Critical or Important findings return to the original implementer, with fresh recapture whenever product/manifest output changes.
