# Ranking Prediction Completion Panel Mobile Visual QA Plan

Date: 2026-08-30

## 1. Freeze phase, matrix, and callback contracts

- Fix three legal phases and exclude `false,true`.
- Fix `44 = 24 canonical + 20 interactions`, hosted/root changes zero, and final totals `451/1,010`, `559`, `70,905`, `71,908`.
- Freeze exact-one native activations and exact-zero non-actions without inventing save/share cross-click single-flight semantics.

## 2. TDD actual product behavior

- Add failing production-CSS browser tests at 320/390 for containment, 140px logo pressure fixtures, x-overflow, 44px/full-width actions, focus, dark contrast, local asset loading, reduced motion, and callback ledgers.
- Assert resize/theme/hover/focus/pressed/Tab/ShiftTab/mount/remount zeros before resets and complete/save/share pointer/Enter/Space exact-one behavior.
- Prove fast double-complete is one and illegal phase mapping is rejected.
- Apply only the smallest leaf-local product fix justified by RED. Shared TeamLogo/Button and caller logic remain unchanged.
- Immediately verify compile, all 153 budgets, CSS byte non-growth, and that forbidden generated reports were not intentionally regenerated or staged.

## 3. TDD exact adapter and companion

- Add exact 24 canonical and 20 interaction catalog/adapter tests.
- Implement one lower-camel QA-only controlled companion and exact original component mapping.
- Fail closed for every unapproved phase/data/theme/interaction/target/action/module combination.
- Prove QA paths and IDs do not appear in production chunks.

## 4. Register arithmetic and package regression

- Change only the direct CompletionPanel manifest entry from pending to registered; keep hosted pending.
- Verify final arithmetic and no root scenario changes.
- Add the focused actual test exactly once to ranking and pre-harness scripts, prove omission and duplication RED, and record post pre/full counts.

## 5. Capture exact evidence

- Warm up outside final paths.
- Capture exactly 44 rows at 320x844, fresh/expand-tall-flow, after local trophy/logo assets load.
- Verify attempt one, failed/recovered zero, 20 complete interaction flags, unique/nonzero/hash-complete PNGs, report SHA, 44-PNG aggregate, and legal equal-image groups.
- Verify prior reports/PNGs and parent/root evidence have zero diff.

## 6. Regression and clean closure

- Run focused actual/mutation tests, ranking/shared primitive regressions, catalog/adapter/contracts, state arithmetic, pre/full harness, production isolation/import/audit/reflow, DOM probe, TypeScript, non-report-generating production compile/CSS/budget checks, Priority 0 validation, and scoped diff checks.
- Reproduce committed dependencies, focused tests, exact totals, TypeScript, production isolation, CSS/budgets, and artifact hashes from final HEAD without overwriting forbidden main reports.
- Preserve all concurrent and known global residuals; finish with index zero and task-owned diff zero.

## 7. Independent review

- A separate reviewer checks exact phase/matrix math, callback ledgers before reset, real mutation effectiveness, mobile visuals/assets, evidence, package integration, CSS, production isolation, scope, concurrency, and policy.
- Critical or Important findings return to the original implementer. Product, manifest, or interaction-output changes require fresh affected capture.
