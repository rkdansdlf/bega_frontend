# Mate Mobile Date Filter Mobile Visual QA Plan

Date: 2026-08-30

## 1. Freeze the boundary and arithmetic

- Fix MobileDateFilter direct at `55`, its visible hosted alias at the existing four Mate host IDs, and Mate root at the existing six IDs.
- Keep DateRail direct `106` unchanged and DateRail hosted pending.
- Preserve controller/API/query/auth semantics, DateRail/concurrent Landing work, staged 99 paths, and the CSS 35-byte starting headroom.

## 2. TDD the product behavior

- Add failing actual-mount assertions for 42px targets, 320/390 local scroll reachability, badge/heading overlap, document overflow, ArrowRight, Tab, outside-range clearing, and callback count isolation.
- Make only the allowed MobileDateFilter product edits: emitted 44px height, stable test IDs/ARIA, and emitted pressed/motion utilities.
- Prove a reciprocal target mutation and a missing callback are RED, then restore actual Chromium `GREEN`.
- Run the production build and all 153 CSS/bundle budgets immediately after product GREEN.

## 3. TDD the direct adapter and QA-only companion

- Add exact tests for five data fixtures, 36 canonical states, 19 interactions, and total `55`.
- Implement a lower-camel stateful companion through explicit `moduleFile`, while preserving the original component ID/file.
- Fail closed for unknown and mismatched component/module/export/data/selection/target/action combinations.
- Prove the companion and wrapper identifiers are absent from all production chunks.
- Add only Mobile-owned hunks to the latest shared adapter/catalog files; preserve DateRail and Landing hunks byte-for-byte.

## 4. Register hosted evidence and exact totals

- Register `MateListControlsRuntime.tsx#MateMobileDateFilter` with the same four results-fallback/runtime light/dark host IDs.
- Keep DateRail hosted and MateListControlsRuntime direct pending.
- Verify registered `447/1,010`, pending `563`, direct `70,767`, valid `71,770`.

## 5. Capture fresh evidence

- Warm up outside final artifact paths.
- Capture MobileDateFilter `55/55` and replacement Mate root `6/6` at 320x844 with `fresh` and `expand-tall-flow`.
- Verify failed/recovered zero, attempts one, artifact/hash integrity, interaction result and callback evidence, local-scroll positions, legal identical-image groups, and unchanged host IDs.
- Verify DateRail report and screenshot diffs are zero.

## 6. Regression and clean closure

- Run focused MobileDateFilter, Mate root/runtime/controller, DateRail preservation, existing control leaves, GlobalError, and duplicate-call coordinator tests.
- Run contracts, inventory, state generation, pre/full harness, isolation/import/audit/reflow, DOM probe, TypeScript, production build, all 153 budgets, Priority 0 policy, and diff checks.
- Reproduce the slice from a detached checkout at final HEAD, including actual Chromium, exact totals, build, production isolation, and report/PNG hashes.
- Preserve staged 99 paths and fingerprints, concurrent DateRail/Landing changes, and the known Landing-only inventory residual.

## 7. Independent review

- A separate reviewer checks exact state math, callbacks and mutation resistance, touch/scroll/focus behavior, hosted/root evidence, report/PNG integrity, production isolation, clean scope, CSS budget, concurrent-change preservation, and policy.
- Critical or Important findings return to the original implementer. Product, manifest, or interaction-output changes require fresh affected evidence.
