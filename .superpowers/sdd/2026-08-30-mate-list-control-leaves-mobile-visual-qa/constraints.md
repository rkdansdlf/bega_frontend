# Constraints

- Scope: direct MateStatusTabs, MateSortDropdown, MateSeatFilterButtons; Status/Sort hosted aliases; their focused tests/adapters/catalog/manifest/docs/reports/PNGs; replacement Mate root 6 evidence.
- Exact direct matrices: Status `21`, Sort `30`, Seat `28`; total `79`.
- Exact hosted mappings: Status `4`, Sort `4`, fully host-owned with zero valid combinations. Seat hosted remains pending.
- Final totals: registered `445/1,010`, pending `565`, direct `70,712`, valid `71,715`.
- Visual QA-only stateful wrappers call the normal public callback once and update controlled props. Unknown targets/values/actions fail closed.
- Actual mounted tests isolate each action, prove intended callback one/unrelated zero, make reciprocal miswires RED, and prove resize/open/scroll do not replay.
- Status touch targets are at least 44px; Sort menu stays inside 320/390 viewports; Seat toolbar scrolls locally without document overflow.
- Preserve Mate root 6 IDs and recapture all six. Preserve MateListControlsRuntime hosted4 and MateTodayCountBadge hosted11 IDs.
- Do not edit MateListControlsRuntime/controller/hooks, PlainMenu, shared Button, resolver/static-data modules, API/auth/query semantics, or external baseball data.
- No new global CSS selectors, Tailwind utilities, or arbitrary values. Starting main-worktree CSS headroom is 35 bytes; run all 153 budgets after first product GREEN.
- Static internal fixtures only. No external baseball API/data, crawling, scraping, web-search repair, or guessed facts.
- Final Status21/Sort30/Seat28/Root6 reports: failed/recovered0, attempts1, unique/nonzero/hash-complete artifacts, all interaction flags true.
- Clean checkout must contain every new dependency. Preserve remaining staged99 and concurrent Landing changes unless a verified prerequisite is explicitly consumed and documented.
- Separate implementer and read-only reviewer; Critical/Important findings return to the original implementer.
