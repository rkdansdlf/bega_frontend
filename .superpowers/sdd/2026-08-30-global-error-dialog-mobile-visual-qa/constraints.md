# Constraints

- Scope: direct `GlobalErrorDialog`, direct `GlobalErrorDialogContent`, hosted `LazyGlobalErrorDialogContent`, focused tests/adapters/catalog/manifest/docs/reports/screenshots, and the minimum fail-closed non-production seam.
- Exact matrices: Root `22`; Content `35`; hosted resolved Root IDs `16` with zero valid combinations.
- Final totals: registered `440/1,010`, pending `570`, direct `70,633`, valid `71,636`.
- Root owns event/listener/lazy coordination; Content owns visible portal controls. Do not duplicate visible interaction states on Root.
- Actual mounted StrictMode/browser tests must isolate every callback/action, prove intended one/unrelated zero, and make reciprocal miswires/listener duplication RED.
- Retry and feedback rapid duplicates invoke once. Viewport expansion is read-only.
- Feedback Visual QA uses an injected static submitter; real reporter/API/network calls are zero.
- Permission is notApplicable. Only legal status/source/retry/feedback combinations are registered.
- Root lazy fallback is a visible named busy status. Portal focus loop, outside sentinel, body scroll restoration, 320px containment, lower-control reachability, and 44px targets are behavioral contracts.
- Do not modify `PlainDialog`, `ErrorFeedbackPanel`, AppQueryProvider/API/auth/payload contracts unless a proven blocker is independently documented.
- Do not generate new global CSS/Tailwind selectors; starting nominal headroom is 35 bytes. Run all 153 budgets immediately after first product GREEN.
- Static `MOCK`/`example.invalid` fixtures only. No external baseball API/data, crawling, scraping, search repair, or guessed facts.
- Final Root `22/22` and Content `35/35`: failed/recovered0, attempts1, unique/nonzero/hash-complete artifacts, all interaction verification flags true.
- Preserve unrelated dirty/staged/concurrent Landing changes. Separate implementer and read-only reviewer; Critical/Important findings return to the original implementer.
