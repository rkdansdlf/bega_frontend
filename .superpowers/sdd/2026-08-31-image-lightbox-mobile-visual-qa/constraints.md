# Constraints

- Scope: direct ImageLightbox, one actual test, exact adapter/catalog tests, one lower-camel QA companion, manifest/state arithmetic, one 42-row report and PNGs, package inclusion, and slice docs.
- Legal data/index: single/only; multiple-three/first-middle-last; maximum-supported(10)/last; broken/only. Mounted means open.
- Exact matrix: `42 = 12 canonical + 30 interactions`; interactions are hover3, focus3, pressed3, click6, keyboard15.
- Final totals: registered `452/1,010`, pending `558`, direct `70,947`, valid `71,950`; host/root unchanged.
- Close/backdrop/Escape/close Enter-Space call close1 only and hide overlay. Previous/next activation calls only its matching callback1 and wraps exactly. Internal/non-action/lifecycle events call all callbacks0 before reset.
- Fast close remains one. Previous/next repeated user actions remain legal; only duplicate dispatch from one DOM event is forbidden.
- Actual checks cover 320x844 and 390x1000 viewport/content containment, x-overflow0, >=44px controls, reduced obstruction, single/multi/max counters, wrapping, broken fallback/reset, propagation, exact body overflow restore, focus trap/restore, light/dark/reduced motion, resize/theme zero, and listener cleanup.
- Product edits require actual RED and are limited to `src/components/ImageLightbox.tsx`. ImageGrid, Cheer parents, icons, focus hook, shared UI, lib/utils, global CSS, and new tooling are forbidden.
- Mutations cover callback swap/duplicate, wrap off-by-one, propagation, listener cleanup, hardcoded overflow, focus trap/initial focus, fallback/reset, touch size, focus/pressed/reduced motion, package missing/duplicate. Replacement must apply and produce behavioral RED.
- Fixtures use data URIs or checked-in local assets only; no external image request.
- QA companion is lower-camel, exact/fail-closed, and absent from production chunks.
- Add the focused test exactly once to pre-harness. Incoming pre/full are `167/167` and `410/410`; no new Cheer script and no global package cleanup.
- Evidence is exact42, f0/r0, attempt1, unique/hash-complete, interaction30 complete, external requests0, report SHA, repo-standard PNG aggregate, and exact legal equal groups.
- Incoming CSS is `255,429/255,500`; CSS must not grow. Build/budget validation must not write main bundle/dist/inventory/classification reports.
- Preserve ImageGrid, dirty Cheer/shared/Mate/prediction/Landing files, package-lock, prior reports/PNGs, and all unrelated state. Re-check scoped ownership before commits.
- No baseball data, external images/API, crawling, scraping, search, or repair.
- Separate implementer and read-only reviewer; Critical or Important findings return to the original implementer.
